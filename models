Feed
    + id
    -entryid not null
    -title not null
    -update not null
    -icon
    -logo
    -rights
    -subtitle

    A Feed contains many entries, can have multiple links can have many authors and contributors, can have multiple categories and is generated by a generator

Entry
    + id
    -entryid not null
    -title not null
    -updated not null
    -content
    -summary
    -published
    -rights
    -source

     An Entry can have multiple links can have many authors and contributors, can have multiple categories and is from a source if is a copy

 Person
    +id
    - name not null
    - email
    - uri

    A Person can be author o contributor